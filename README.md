# Poject Requirements

1. i want to be able to run it on terminal with command `lyra`
2. it will use `SpeechRecognition` with `audiodevice`
   - `sounddevice` will handle the audio recording
   - `SpeechRecognition` will handle Speech Recogintio
3. Basec on what i say it will run a command i want 


